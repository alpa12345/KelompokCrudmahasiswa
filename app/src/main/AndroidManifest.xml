<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tugaskelompok">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreen.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ReadEditDelete.EditActivity" />
        <activity android:name=".ReadEditDelete.DetailMahasiswa" />
        <activity android:name=".ReadEditDelete.LihatData" />
        <activity android:name=".InsertData.TambahActivity" />
        <activity android:name=".DashboardActivity">
        </activity>
    </application>

</manifest>